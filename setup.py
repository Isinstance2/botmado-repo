# setup.py
from setuptools import setup, find_packages

setup(
    name="colamdo_ai",          # Package name
    version="0.1",            # Version
    packages=find_packages(), # Automatically find packages
)
